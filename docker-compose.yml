services:
  imapfilter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: imapfilter
    restart: unless-stopped
    environment:
      IMAPFILTER_INTERVAL: ${IMAPFILTER_INTERVAL:-300}
    volumes:
      - ./config:/etc/imapfilter
      - ./data:/var/lib/imapfilter/data
    command: ["loop"]

  imapfilter-once:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: imapfilter-once
    restart: "no"
    volumes:
      - ./config:/etc/imapfilter
      - ./data:/var/lib/imapfilter/data
    command: ["once"]
